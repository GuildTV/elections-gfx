<div class="singleProfile col-md-12">
  <div class="col-md-8 col-md-offset-2">
    <h2>{{ name }}</h2>
    <h3>{{ position }}</h3>
    <img src="{{ img }}" alt="{{ name }}">
    <h3>Manifesto</h3>
    <h4>{{ manifestoPoints.one }}</h4>
    <h4>{{ manifestoPoints.two }}</h4>
    <h4>{{ manifestoPoints.three }}</h4>
  </div> <!-- / .col-md-8 .col-md-offset-2 -->
</div> <!-- / .singleProfile -->
